<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- ⚠️ Usa SOLO uno de estos según tu compileSdkVersion -->
    <!-- Para Android 13+ (SDK 33 o superior) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Para Android 12 o menor -->
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElAsadosSteaks">

        <!-- ✅ API Key de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="TU_API_KEY_REAL"/>

        <!-- ✅ FileProvider para cámara real -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.asados_steaks.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <!-- ✅ Actividades -->
        <activity android:name=".SeleccionarUbicacionMapActivity" android:exported="false" />
        <activity android:name=".PerfilUsuarioActivity" android:exported="false" />
        <activity android:name=".NuevoPedidoActivity" android:exported="false" />
        <activity android:name=".CambiarContrasenaActivity" android:exported="false" />
        <activity android:name=".VerificarClaveActivity" android:exported="false" />
        <activity android:name=".ReenviarClaveActivity" android:exported="false" />
        <activity android:name=".RecuperarClaveActivity" android:exported="false" />
        <activity android:name=".DashboardRepartidorActivity" android:exported="false" />
        <activity android:name=".PedidosActivosActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".DashboardUsuarioActivity" android:exported="false" />
        <activity android:name=".ResetPasswordActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
